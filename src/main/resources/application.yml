spring:
  datasource:
    username: root
    password: 1234
    url: jdbc:mariadb://localhost:3306/board
  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true